cli:
  status:
    header_game: "GAME"
    header_last_backup: "LAST BACKUP"
    header_size: "SIZE"
    header_status: "STATUS"
    no_save_detected: "⚠ no save detected (see extra_save_paths in config.toml)"
    never_synced: "never synced"
    idle: "up to date"
    running: "syncing..."
    error: "error"
    none: "-"
  sync:
    triggered: "sync triggered"
  history:
    header_game: "GAME"
    header_when: "WHEN"
    header_destination: "DESTINATION"
    header_ok: "OK?"
    yes: "yes"
    no: "no"
  common:
    unexpected_response: "unexpected response from daemon"
    daemon_not_running: "could not connect to the daemon at %{path} — is it running? (`systemctl --user status playsyncd`)"
    daemon_closed_connection: "the daemon closed the connection without responding"
  cloud:
    not_connected: "not connected yet — run `playsync cloud connect %{provider}` first"
    test_upload_done: "test upload complete — check drive.google.com (or Box)"
    unknown_provider: "unknown provider: %{other} (use `google-drive` or `box`)"
    setup:
      intro_google: "Create an OAuth client ID (type \"Desktop app\") at https://console.cloud.google.com/apis/credentials, then add http://localhost:8085 as a redirect URI — the Client ID and Client Secret are shown on the same page"
      intro_box: "Create a Custom App at https://app.box.com/developers/console with \"User Authentication (OAuth 2.0)\" enabled, then add http://localhost:8086 as a redirect URI — the Client ID and Client Secret are on the app's Configuration tab"
      prompt_client_id: "Client ID: "
      prompt_client_secret: "Client Secret: "
      empty_client_id: "Client ID cannot be empty"
      empty_client_secret: "Client Secret cannot be empty"
      saved: "credentials saved"
      next_step: "run `playsync cloud connect %{provider}` to authorize"
  restore:
    game_not_found: "game with AppID %{app_id} not found (see `playsync status`)"
    no_save_path: "\"%{name}\" has no known save folder (neither live nor in backup history)"
    used_history_warning: "warning: current save folder for \"%{name}\" was not found on disk — using the path from the last successful backup"
    choose_path_index: "\"%{name}\" has %{n} save folders — choose one with --path-index:"
    invalid_path_index: "--path-index %{idx} is invalid — \"%{name}\" only has %{n} save folder(s)"
    no_versions_found: "no backup version found for \"%{name}\" in that source"
    versions_available: "versions available for \"%{name}\" (most recent last):"
    confirm_prompt: "Restoring \"%{name}\" (%{file_name}, backup %{source_label}) will DELETE and overwrite:\n  %{target}"
    continue_prompt: "Continue? [y/N] "
    cancelled: "cancelled"
    success: "Successfully restored: %{target}"
    version_not_found: "version \"%{name}\" not found (use --list-versions to see the available ones)"
    no_backup_for_slot: "no backup found for this game/folder/source"
    provider_not_connected: "%{provider} not connected — run `playsync cloud connect` first"
    local_backup_not_found: "local backup not found at %{path}"
    download_failed: "failed to download %{remote_path}"
    delete_failed: "failed to delete %{path}"
    extract_failed: "failed to extract backup"
    label_manual: " (manual)"
    label_session: " (session: %{label})"
    label_short_session: " (⚠ short session — %{label})"
    unit_min: "%{n}min"
    unit_sec: "%{n}s"
  config:
    auto_restore_enabled: "auto-restore on launch: enabled"
    auto_restore_disabled: "auto-restore on launch: disabled"
    language_set: "language set to %{language}"
    unknown_language: "unknown language: %{lang} (available: %{available})"
    invalid_auto_restore_state: "\"%{value}\" is invalid — use \"on\" or \"off\""
tui:
  actions:
    sync_now: "Sync now (local + cloud backup)"
    pull_from_cloud: "Download from cloud (local only, doesn't touch save)"
    restore_local: "Restore to game (from local backup)"
    restore_from_cloud: "Download from cloud and restore to game"
  game_not_found: "game (AppID %{app_id}) not found"
  sync_triggered: "sync for \"%{name}\" triggered"
  cloud_provider_not_configured: "no cloud provider configured (run `playsync cloud connect`)"
  error_prefix: "Error: %{err}"
  history_fallback_prefix: "warning: current save folder not found on disk, using path from last backup — "
  pulled_from_cloud: "Downloaded from cloud to %{path}"
  restored_local: "Restored (local backup, %{file_name}) to %{path}"
  restored_cloud: "Restored (cloud, %{file_name}) to %{path}"
  invalid_path_index: "invalid save index (%{path_index})"
  action_menu_title: " %{title} — choose an action  ([Esc] cancel) "
  path_choice_title: " %{title} — which save folder?  ([Esc] cancel) "
  version_choice_title: " %{title} — which version to restore? (most recent marked below)%{warning}  ([Enter] choose [Esc] cancel) "
  version_choice_warning: " — ⚠ current save folder not found, target comes from history"
  confirm_title: " Confirm — [Enter]/[y] confirm  [Esc]/[n] cancel "
  confirm_body: "%{title}\n\n%{label}\n\nVersion: %{file_name}\n%{history_warning}\nThis will DELETE the game's current save.\n\n[Enter] or [y] confirm   [Esc] or [n] cancel"
  confirm_history_warning: "\n⚠ current save folder NOT found — target comes from history (may no longer be valid)\n"
  info_title: " PlaySync — [any key] continue "
  info_body_suffix: "\n\n(any key continues)"
  table:
    header_game: "Game"
    header_last_backup: "Last backup"
    header_size: "Size"
    header_status: "Status"
    no_save_detected: "⚠ no save detected"
    title: " PlaySync — [↑↓] navigate  [Enter] actions  [s] sync all  [r] refresh  [q] quit  [c] settings "
  settings:
    title: " Settings — [Esc] back "
    cloud_provider_label: "Cloud provider"
    cloud_provider_none: "(not configured)"
    auto_restore_label: "Auto-restore on game launch"
    state_on: "ON"
    state_off: "OFF"
    language_label: "Language"
    credentials_label: "Configure cloud credentials (Client ID/Secret)"
    hint: " [Enter] change  [↑↓] navigate  [Esc] back "
  credentials:
    provider_choice_title: " Which provider?  ([Esc] cancel) "
    input_title: " %{provider} credentials — [Enter] next field / save  [Backspace] delete  [Esc] cancel "
    body: "%{id_marker}Client ID: %{client_id}\n%{secret_marker}Client Secret: %{client_secret}"
    saved: "credentials saved for %{provider}"
